<br>
<p style="text-align: center; color: var(--light-quarter)"><a href="/">© Boise Gems Drum & Bugle Corps 2026</a></p>
<br>
<div class="socials">
  <a href="https://www.instagram.com/boisegems/" target="_blank" style="background-color: rgb(221, 42, 123);">
    <ion-icon name="logo-instagram"></ion-icon>
  </a>
  <a href="https://www.facebook.com/theboisegems/" target="_blank" style="background-color: #3b5998;">
    <ion-icon name="logo-facebook"></ion-icon>
  </a>
  <a href="https://www.youtube.com/@gemsdbc" target="_blank" style="background-color: #CD201F;">
    <ion-icon name="logo-youtube"></ion-icon>
  </a>
</div>
<div style="text-align: center;">
    <h4 style="font-weight: 700; text-transform: uppercase; color: var(--light-quarter);"></h4>
    <br>
</div>
<div style="display: grid; grid-template-columns: 1fr 1fr 1fr 1fr; text-align: center;" class="lower-images">
    <div>
        <a href="https://zildjian.com/" target="_blank">
            <img src="/img/ui/zildjian.png" alt="Zildjian logo" style="width: 80%; max-width: 196px; padding: 8px;">
        </a>
    </div>
    <div>
        <a href="https://vicfirth.com/collections/vic-firth-category-mallets-keyboard-balter-mallets?fbclid=IwY2xjawQTEntleHRuA2FlbQIxMABicmlkETJ3TnlVd1czcFZOZGRvQVdEc3J0YwZhcHBfaWQQMjIyMDM5MTc4ODIwMDg5MgABHoOWnMnoH78a1woVIe5JbCpdm49oom9xRS04Cq4EW1GId58m6FZaKsUUIx3c_aem_MHlyxNYe-5WDF9bwA_47bQ" target="_blank">
            <img src="/img/ui/balter.png" alt="Balter Mallets logo" style="width: 80%; max-width: 196px; padding: 8px;">
        </a>
    </div>
     <div>
        <a href="https://vicfirth.com/" target="_blank">
            <img src="/img/ui/vicfirth.png" alt="Vic Firth logo" style="width: 80%; max-width: 196px; padding: 8px;">
        </a>
    </div>
    <div>
        <a href="https://pageantryinnovations.com/" target="_blank">
            <img src="/img/ui/pi.png" alt="Pageantry Innovations logo" style="width: 80%; max-width: 512px; padding: 8px;">
        </a>
    </div>
</div>
<br>
<br>
<div style="display: grid; grid-template-columns: 1fr 1fr 1fr 1fr; text-align: center;" class="lower-images">
    <div>
        <a href="https://www.ultimatedrillbook.com/" target="_blank">
            <img src="/img/ui/udb.png" alt="Ultimate Drill Book logo" style="width: 80%; max-width: 196px; padding: 8px;">
        </a>
    </div>
    <div>
        <a href="https://www.marchingmapper.com/" target="_blank">
            <img src="/img/ui/marchingmapper.png" alt="Marching Mapper logo" style="width: 80%; max-width: 196px; padding: 8px;">
        </a>
    </div>
     <div>
        <a href="https://www.dci.org/" target="_blank">
            <img src="/img/ui/dci.png" alt="Dci logo" style="width: 80%; max-width: 196px; padding: 8px;">
        </a>
    </div>
    <div>
        <a href="https://www.wgi.org/" target="_blank">
            <img src="/img/ui/wgi.png" alt="Wgi logo" style="width: 80%; max-width: 196px; padding: 8px;">
        </a>
    </div>
</div>
<div style="display: grid; grid-template-columns: 1fr 1fr 1fr 1fr; text-align: center;" class="lower-images">
    <div>
        <a href="https://www.discovervibes.com/" target="_blank">
            <img src="/img/ui/vibes-logo.png" alt="Vibes logo" style="width: 80%; max-width: 196px; padding: 8px;">
        </a>
    </div>
</div>
<br>
<div class="lower-links">
    <br>
    <div style="display: grid; grid-template-columns: 1fr 1fr;">
        <div>
            <a href="/">Home</a><br>
            <a href="/news">News</a><br>
            <a href="/contact">Contact</a><br>
            <a href="/privacy">Privacy Policy</a><br>
            <a href="/whistleblower">Whistleblower</a><br>
        </div>
        <div>
            <a href="/our-history-about">Our History</a><br>
            <a href="/boise-gems-corps-about">Boise Gems Drum &amp; Bugle Corps</a><br>
            <a href="/boise-gems-independent">Boise Gems Independent</a><br>
            <a href="/shows">Shows</a><br>
            <a href="/donate">Donate</a><br>
        </div>
    </div>
    <br>
    <a href="https://info.chrispricemusic.net" style="font-size: medium;">Powered by Chris Price Music</a>
</div>
<br>
<br>



<script>
    function copyText(element) {
        const text = element.innerText || element.textContent;

        console.log(text)

        navigator.clipboard.writeText(text).then(() => {
            showMessage(text + " copied to clipboard!")
        }).catch(err => {
            showMessage("Failed to copy")
        })
    }

    function showMessage(text) {
        const message = document.getElementById("stickyMessage")

         message.style.display = 'block'

        message.innerHTML = text;
        message.classList.remove("sticky-animation")
        void message.offsetWidth;
        message.classList.add("sticky-animation")

        message.addEventListener("animationend", () => {
            message.style.display = 'none'
        }, {once : true})
    }

    function messageReceived(message) {
        showMessage(message);
    }

    window.addEventListener("DOMContentLoaded", async () => {
        try {
            const contentType = document.contentType || (document.querySelector('meta[http-equiv="Content-Type"]')?.content ?? "")

            if(contentType.includes("application/json")) {
                const json = await fetch(window.location.href).then(res => res.json())
                messageReceived(json)
            } 
        }   catch (err) {
                console.log("error")
        }

          const fadeElements = document.querySelectorAll('.fade-in');
        let lastScrollY = window.scrollY;
        function checkVisibility() {
        const centerY = window.innerHeight * 0.75;
        fadeElements.forEach(el => {
        const rect = el.getBoundingClientRect();
        const elCenter = rect.top + rect.height / 2;
        const scrolledDown = window.scrollY > lastScrollY;
        if (elCenter <= centerY) {
        el.classList.add('visible');
        } else {
            el.classList.remove('visible')
        }
        });
        lastScrollY = window.scrollY;
        }
        window.addEventListener('scroll', checkVisibility);
        window.addEventListener('resize', checkVisibility);
        checkVisibility(); // run on load too

        document.querySelectorAll('.carousel').forEach((carousel) => {
            const slides = carousel.querySelectorAll('.esto-pic')
            let index = 0;
            let delay = parseInt(carousel.dataset.delay) || 3000;

            if(slides.length === 0) return;

            slides[0].classList.add('active')

            setInterval(() => {
                slides[index].classList.remove('active')
                index = (index + 1) % slides.length;
                slides[index].classList.add('active')
            }, delay)
        })
    })

    
    const wigglers = document.querySelectorAll('.wiggle')


    function updateShift(xFactor, yFactor){
        
        
        
        wigglers.forEach(el => {
            const MAX_SHIFT = el.getAttribute('max-shift');
            const xShift = (xFactor - 0.5) * 2 * MAX_SHIFT;
            const yShift = (yFactor - 0.5) * 2 * MAX_SHIFT;
            console.log("UPDAE")
            el.style.transform = `translate(${xShift}px, ${yShift}px)`
        })

    }

    window.addEventListener('mousemove', e => {
        const xFactor = e.clientX / window.innerWidth;
        const yFactor = e.clientY / window.innerHeight;

        console.log("X:" + xFactor)
        updateShift(xFactor, yFactor)
    })
    
    window.addEventListener('deviceorientation', e => {
        if(e.beta == null || e.gamma == null) return;

        const xFactor = (e.gamma + 90) / 180;
        const yFactor = (e.beta + 180) / 360
        updateShift(xFactor, yFactor)
    })

</script>

<script>
  const overlay = document.getElementById('confirm-overlay');
  const yesBtn = document.getElementById('confirm-yes');
  const noBtn = document.getElementById('confirm-no');

  let nextHref = '#';

  document.querySelectorAll('.confirm-btn').forEach(btn => {
    btn.addEventListener('click', (e) => {
      e.preventDefault();
      nextHref = btn.getAttribute('next') || '#';
      overlay.style.display = 'flex';
    });
  });

  yesBtn.addEventListener('click', () => {
    window.location.href = nextHref;
  });

  noBtn.addEventListener('click', () => {
    overlay.style.display = 'none';
    nextHref = '#';
  });
</script>

<% if(typeof flashMessage !== "undefined") { %>
    <script>
        showMessage("<%= flashMessage %>")
    </script>
<% } %>